// The application MUST provide this configuration
com.okune.database = {
	mongo = {
		host = "localhost"
		port = 27017
		database = "test"
		authMode = "off"	
	}
	
	postgres = {
		dataSourceClass = org.postgresql.ds.PGSimpleDataSource
		properties = {
			serverName = "localhost" 
			portNumber = 5432
			databaseName = "test"
			user = "postgres"
			password = "postgres"
		}
		numThreads = 10
  	}
}